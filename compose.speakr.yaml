include:
  - compose.cuda.yaml
services:
  speakr:
    image: learnedmachine/speakr:latest
    container_name: speakr
    env_file:
      - .env
    ports:
      - 8899:8899
    tty: true
    volumes:
      - ${SPEAKR_DATA}:/data
    restart: unless-stopped
